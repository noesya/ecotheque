<%= content_for :title, "#{User.model_name.human(count: 2)} (#{@users.total_count})" %>

<%= render 'filters', current_path: admin_users_path %>

<%= render 'admin/users/list', users: @users %>
<%= paginate @users, theme: 'bootstrap-5' %>

<% content_for :buttons do %>
  <%= link_to t('admin.export'), request.params.merge(format: "xlsx"), class: button_classes %>
<% end %>