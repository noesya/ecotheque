<%= t(
      'mailers.export_ok.body_short',
      kind: User.model_name.human(count: 2).downcase,
      export_url: admin_file_export_url(@export),
      expires_at: l(@export.expires_at)
    ).html_safe %>